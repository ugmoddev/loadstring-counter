services:
  - type: web
    name: loadstring-counter
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: NODE_ENV
        value: production
    plan: free
    healthCheckPath: /health
    autoDeploy: true
