# Loadstring Counter System

Hệ thống đếm số lần một script được gọi qua `loadstring(game:HttpGet(...))` trong môi trường Lua (Roblox, etc.). Toàn bộ API trả về Raw Response (Plain Text hoặc JSON), không có giao diện HTML.

## Cài đặt

```bash
npm install
npm start